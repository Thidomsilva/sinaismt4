import { LucideProps } from 'lucide-react';
declare module 'lucide-react' {
    export const CalendarClock: (props: LucideProps) => JSX.Element;
    export const Timer: (props: LucideProps) => JSX.Element;
    export const SearchX: (props: LucideProps) => JSX.Element;
    export const AlertCircle: (props: LucideProps) => JSX.Element;
}
